<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
            font-size: 12px;
        }

        th, td {
            border: 1px solid #000;
            padding: 5px;
            text-align: left;
        }

        th {
            background-color: #e4e4e4;
        }
    </style>
</head>
<body>
    <h2>Reporte Orgánico - Exportación</h2>

    <table>
        <thead>
            <tr>
                <th>Servicio</th>
                <th>Nomenclatura</th>
                <th>Cargo</th>
                <th>Orgánico Aprobado</th>
                <th>Orgánico Efectivo</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($datos as $item)
                <tr>
                    <td>{{ $item->servicio_organico }}</td>
                    <td>{{ $item->nomenclatura_organico }}</td>
                    <td>{{ $item->cargo_organico }}</td>
                    <td>{{ $item->organico_aprobado }}</td>
                    <td>{{ $item->organico_efectivo }}</td>
                </tr>
            @endforeach
        </tbody>
    </table>
</body>
</html>
